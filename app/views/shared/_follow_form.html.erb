<div id="follow_form">
  <% if logged_in? && !current_user?(@user) %>
    <% if @follow.present? %>
      <%= render "shared/unfollow" %>
    <% else %>
      <%= render "shared/follow" %>
    <% end %>
  <% end %>
</div>
