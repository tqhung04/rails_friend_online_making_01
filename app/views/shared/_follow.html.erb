<%= form_for :conection, url: conections_path, remote: true do |f| %>
  <%= f.hidden_field :recipient_id, value: @user.id %>
    <%= f.submit t(".add_friend"), class: "btn_sign_up btn btn-sm btn-primary"%>
<% end %>
